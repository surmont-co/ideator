# =============================================================================
# WorkOS Authentication Configuration
# =============================================================================
# Obține aceste credențiale din WorkOS Dashboard (https://dashboard.workos.com/)
# Vezi WORKOS_SETUP.md pentru instrucțiuni detaliate de configurare

# WorkOS Client ID (ex: client_...)
WORKOS_CLIENT_ID="your_workos_client_id_here"

# WorkOS API Key (ex: sk_...)
WORKOS_API_KEY="your_workos_api_key_here"

# WorkOS Redirect URI (URL-ul unde WorkOS trimite utilizatorul după autentificare)
# Pentru dezvoltare locală: http://localhost:3000/api/auth/callback
# Pentru producție/Docker: https://yourdomain.com/api/auth/callback
# Docker default: https://ideator.surmont.co/api/auth/callback
WORKOS_REDIRECT_URI="http://localhost:3000/api/auth/callback"

# Parolă pentru criptarea cookie-urilor de sesiune (minim 32 caractere)
# Generează una folosind: openssl rand -base64 32
WORKOS_COOKIE_PASSWORD="your_secure_cookie_password_at_least_32_chars_long"

# =============================================================================
# Database Configuration
# =============================================================================
# Calea către fișierul SQLite (implicit: database.sqlite în rădăcina proiectului)
# În Docker, aceasta este setată automat la /app/data/database.sqlite
DATABASE_URL="database.sqlite"

# =============================================================================
# AI/LLM Configuration (pentru sumarizare și similitudine propuneri)
# =============================================================================
# Gemini API Key (OBLIGATORIU pentru funcționalitatea de sumarizare AI)
# Obține de la: https://aistudio.google.com/app/apikey
GEMINI_API_KEY="your_gemini_api_key_here"

# Model Gemini (opțional, default: gemini-2.5-flash-lite)
GEMINI_MODEL="gemini-2.5-flash-lite"

# OpenAI Configuration (opțional, alternativă la Gemini)
# OPENAI_API_KEY="your_openai_api_key_here"
# OPENAI_MODEL="gpt-4"

# =============================================================================
# Localization & Regional Settings
# =============================================================================
# Limba implicită pentru interfață și sumarizare AI
# Valori acceptate: "en" (English) sau "ro" (Română)
# Default: "en"
LOCALE="en"

# =============================================================================
# Session & Security Configuration
# =============================================================================
# Domeniul pentru cookie-ul de sesiune (opțional)
# Folosit pentru a seta domeniul cookie-ului în producție/Docker
# Docker default: .surmont.co
# Pentru dezvoltare locală, lasă nesetat sau comentat
# SESSION_COOKIE_DOMAIN=".yourdomain.com"

# =============================================================================
# Docker Configuration
# =============================================================================
# Portul extern pe care aplicația va fi accesibilă când rulează în Docker
# Docker va mapa acest port la portul intern 3000 al containerului
# Default: 3011
APP_PORT="3011"

# =============================================================================
# Environment
# =============================================================================
# Mediul de rulare (development/production)
# Setat automat de Next.js, dar poate fi suprascris
# NODE_ENV="development"
