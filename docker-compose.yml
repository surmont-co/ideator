services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
    volumes:
      - database_data:/app/database.sqlite
    restart: unless-stopped
volumes:
  database_data:
