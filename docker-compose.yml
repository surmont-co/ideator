services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
    volumes:
      - ./database.sqlite:/app/database.sqlite
    restart: unless-stopped
