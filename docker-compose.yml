services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
      DATABASE_URL: /app/data/database.sqlite
    volumes:
      - database_data:/app/data
    restart: unless-stopped
volumes:
  database_data:
